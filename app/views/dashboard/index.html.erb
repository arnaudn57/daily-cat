<h1 class="d-flex justify-content-center mt-3"><%= "Chalut #{current_cat.username} 😻" %></h1>
<h2 class="d-flex justify-content-center mt-3"><%= "Tu as #{@reservations.count} réservations en attente !" %></h2>
<div class="align-center text-center text-dark mt-2 p-5 container_card d-flex " style="max-width: 60rem;">
          <% @reservations.each do |res| %>
            <div class="card-product w-50">
              <%= cl_image_tag res.product.photos.first.key %>
              <div class="card-product-infos">
                <h2><%= res.product.title %></h2>
                <p><%= res.product.description %></p>
              </div>
            <div class="delete-btn">
              <%= link_to "Confirmer",
              reservation_path(res),
              method: :delete,
              data: { confirm: "La vente a-t-elle bien eu lieu ?" } %>
              </div>
            </div>
      <% end %>
</div>
